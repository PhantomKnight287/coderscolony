{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "dev": {
      "cache": false,
      "outputMode": "full"
    },
    "build": {
      "cache": true,
      "outputMode": "full",
      "outputs": ["dist/**", ".next/**"]
    }
  }
}
